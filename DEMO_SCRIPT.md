# Demo 话术脚本（预计 5-8 分钟）

## 演示用论文

tveq.pdf — "Propranolol Enhances the Oncolytic Effect of Newcastle Disease Virus on Canine Mammary Tumor Cell..."
- 真实发表论文
- 检测结果：3 红灯 + 1 橙灯 + 4 黄灯，风险评分 51/100

---

## 第 1 步：开场（30 秒）

> 这是一个学术论文造假检测工具，叫 PaperFraud。它不是 ChatGPT 套壳，而是三层交叉校验引擎——规则引擎 + 统计检验 + LLM 定性分析。我来跑一篇真实论文演示一下。

终端执行：

```bash
paperfraud check tests/fixtures/tveq.pdf --extract-images --output-dir output/demo
```

---

## 第 2 步：看终端输出（1 分钟）

滚动终端，重点指：

> 可以看到，这篇论文检出 3 个红灯、1 个橙灯、4 个黄灯，风险评分 51 分，判定为高度可疑。

指三个红灯逐一说明：

> **第一个红灯：正态性声明审计。** 论文 Methods 里写了 "all data met the assumption of normal distribution"——这是个经典统计错误。多组比较时应该检验模型残差的正态性，而不是原始数据。原始数据在不同处理组间存在效应的情况下，不可能符合单一正态分布。这不是我编的规则，是统计学教材里的基础知识。

> **第二个红灯：统计推断谬误。** 论文报告 P > 0.05 之后写了 "again indicating"——用不显著的结果推断"无效应"或"等效"，这是经典谬误。P > 0.05 只说明没检测到差异，不能证明没有差异。

> **第三个红灯：标题-结论不一致。** 标题用了 "Enhances" 这个疗效动词，但 Results 里有 14 处写了 no significant differences，其中 10 处涉及主要终点。这个结论夸大的程度相当严重。

指橙灯：

> **尾数偏好**——这篇论文报告的数值里，末尾是 0 或 5 的占了 35.5%，卡方检验 P 值趋近于 0。这不一定是造假，但提示数据可能经过了人为取整或凑整。

---

## 第 3 步：打开 Web UI — 检测总览（1 分钟）

浏览器打开 http://localhost:8501，输入报告路径 `output/demo/report.json`。

> Web 审查工作台提供了可视化界面，方便人工复核。总览页按红橙黄绿显示所有检测项，每个信号都附带证据链和人工复核指引。

点击侧边栏 **🔴 Red 信号**：

> 每个红灯展开后能看到原文摘录，精确到段落。比如这个正态性问题，直接引用了 Methods 里的原句，并且给出了具体的人工复核指引。

---

## 第 4 步：图像取证（1.5 分钟）

点击 **🖼️ 图像取证**。

> 图像检测这部分我们做了三个技术：LUT 伪彩映射、ELA 误差水平分析、Clone Detection 克隆检测。但我需要先说明一个设计决策——

> **图像检测只生成辅助可视化图，不做自动判定。** 原因是在学术打假这个场景下，80% 的 AI 图像检测准确率是不够的。一个假阳性就可能毁掉一个研究者的职业生涯。所以我们让工具做"放大镜"，最终的判断留给人来完成。

演示 LUT：

> 这是 LUT 伪彩映射。原始灰度图用 Fire、Royal、Viridis 三种伪彩色重新渲染，肉眼难以察觉的亮度和纹理差异在伪彩色下会变成明显的颜色断层。如果有拼接或擦除痕迹，这里会很明显。

切换到 ELA：

> ELA 利用了 JPEG 压缩的特性。图片被多次编辑保存后，不同区域的压缩误差不同。编辑过的区域在高压缩率下会呈现明显的高亮——这些高亮区域提示可能存在外部粘贴内容。

切换到 Clone Detection：

> Clone Detection 用 pHash 滑动窗口检测图像内的复制粘贴。红色连线标记出像素级相似的两个区域。这在 Western Blot 或显微图中可以用来发现 copy-paste 伪造。

---

## 第 5 步：LLM 审查 + PubPeer 草稿（30 秒）

如果你配了 API Key，点 **🤖 LLM 审查**：

> LLM 审查会综合所有检测结果，做降噪和交叉验证。比如本福特定律飘红，但如果 Methods 中确认数据经过了 Log2 标准化，LLM 会把它降级为绿灯——因为标准化后的数据不适用本福特。

点 **📝 PubPeer 草稿**：

> 自动生成中文审稿意见，可以直接发表到 PubPeer。所有结论引用具体证据，不是"AI 说这篇造假"这种黑箱判断。

---

## 第 6 步：收尾 — 核心差异化（1 分钟）

切回终端或展示 `docs/design.md`：

> 总结一下这个系统的三个核心设计决策：

> **第一，三层必须分开。** 规则引擎和统计分析的输出是 100% 确定性的，有数学公式支撑。LLM 只在第三层做定性判断和跨信号关联。如果只用 LLM 一把梭，面试官一眼就知道你是外行。

> **第二，规则来自学科知识，不是调 API。** 比如"Western Blot 需要 loading control""n=2 时方差估计无意义""P > 0.05 不能推断无效应"——这些是生物学和统计学的基本常识，不是 NLP 能解决的。知道该提取什么，比怎么提取重要得多。

> **第三，诚实边界。** 图标出不检测的东西：完全内部自洽的伪造数据、论文工厂的专业出品、跨论文系统造假——这些是目前技术无法可靠检测的。所有信号仅供参考，最终判定需要领域专家人工裁决。

---

## 备用：如果面试官追问

**"这个和直接用 ChatGPT 分析论文有什么区别？"**

> ChatGPT 分析论文是黑箱——你不知道它依据什么规则判断。而我们的规则引擎每条规则都是可追溯的：statcheck 用 scipy 反算 P 值，GRIM 检验基于整数均值的粒度约束，本福特基于首位数字分布。这些都有数学公式和参考文献支撑。LLM 只是第三层的定性分析，前两层的结论不需要 LLM 也能独立成立。

**"你为什么不做自动图像判定？"**

> 技术上可以做，但准确率天花板在 85-90% 左右。学术打假场景下，10% 的假阳性意味着每审查 10 篇论文就有 1 篇被误标。这个代价太高了。我们把图像检测定位为"辅助放大镜"而非"自动判决器"是经过深思熟虑的。

**"你怎么知道这篇论文不是假阳性？"**

> 三条红灯各有独立证据且互相印证。统计方法错误是第一层（方法学不规范），统计推断谬误是第二层（结论超出数据支持范围），标题结论不一致是第三层（叙事夸大）。三条信号指向同一个问题——统计严谨性不足。这比单一信号的可信度高得多。
